version: '3.8'

services:
  mysql:
    image: mysql:5.7
    container_name: reits_mysql
    environment:
      MYSQL_ROOT_PASSWORD: root_password  # 请记住这个密码，后续配Python要用
      MYSQL_DATABASE: reits_db
    ports:
      - "3306:3306"
    volumes:
      - ./docker_data/mysql:/var/lib/mysql
    networks:
      - reits_net

  elasticsearch:
    image: elasticsearch:7.17.10
    container_name: reits_es
    environment:
      - discovery.type=single-node
      - "ES_JAVA_OPTS=-Xms1g -Xmx1g"  # 限制ES内存占用，防止WSL卡死
      - xpack.security.enabled=false
    ports:
      - "9200:9200"
    volumes:
      - ./docker_data/es:/usr/share/elasticsearch/data
    networks:
      - reits_net

  etcd:
    container_name: reits_etcd
    image: quay.io/coreos/etcd:v3.5.5
    environment:
      - ETCD_AUTO_COMPACTION_MODE=revision
      - ETCD_AUTO_COMPACTION_RETENTION=1000
      - ETCD_QUOTA_BACKEND_BYTES=4294967296
      - ETCD_SNAPSHOT_COUNT=50000
    volumes:
      - ./docker_data/etcd:/etcd
    command: etcd -advertise-client-urls=http://127.0.0.1:2379 -listen-client-urls http://0.0.0.0:2379 --data-dir /etcd
    networks:
      - reits_net

  minio:
    container_name: reits_minio
    image: minio/minio:RELEASE.2023-03-20T20-16-18Z
    environment:
      MINIO_ACCESS_KEY: minioadmin
      MINIO_SECRET_KEY: minioadmin
    volumes:
      - ./docker_data/minio:/minio_data
    command: minio server /minio_data --console-address ":9001"
    ports:
      - "9000:9000"
      - "9001:9001"
    networks:
      - reits_net

  milvus:
    container_name: reits_milvus
    image: milvusdb/milvus:v2.2.11
    command: ["milvus", "run", "standalone"]
    environment:
      ETCD_ENDPOINTS: etcd:2379
      MINIO_ADDRESS: minio:9000
    volumes:
      - ./docker_data/milvus:/var/lib/milvus
    ports:
      - "19530:19530"
      - "9091:9091"
    depends_on:
      - etcd
      - minio
    networks:
      - reits_net

networks:
  reits_net:
    driver: bridge